import formatFileSize from './formatFileSize.js';
import formatDuration from './formatDuration.js';

export {
  formatFileSize as __formatFileSize,
  formatFileSize,
  formatDuration as __formatDuration,
  formatDuration,
};
